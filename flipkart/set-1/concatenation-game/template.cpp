#include <bits/stdc++.h>
using namespace std;

void concatenationGame(vector<long long> &a)
{
}

int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);

	int n;
	cin >> n;

	vector<long long> a(n);
	for(auto &ele : a)
		cin >> ele;

	concatenationGame(a);
	return 0;
}


